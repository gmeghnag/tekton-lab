apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: greetings-el
spec:
  serviceAccountName: pipeline
  triggers:
  - bindings:
    - ref: greetings-tb
    template:
      ref: greetings-tt
